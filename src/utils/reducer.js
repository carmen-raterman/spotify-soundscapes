export const initialState = {
    token: null, //null initially, but this is for spotify token
};

const reducer = (state, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default reducer;